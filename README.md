# ThirdBrain — 세종시 12대 섹터 진단·공약 네트워크

「세종시 진단 12대 섹터 정리」의 **97개 진단**과 브리핑 계획표·공약자료집의 **54개 공약**을
하나의 망으로 잇는다. 배치는 **맞물린 두 개의 원환**이고, 두 원이 겹쳐 만드는
렌즈(vesica)가 곧 수렴점이다.

```
             ┌ 팀 166       가장 바깥
             ├ 과 76
바깥 궤도 ─┤ 진단 97  ○     문제는 바깥에 드러난다
             ├ 섹터 12
안쪽 궤도 ─┤ 공약 54  ◇     해법은 안에서 잇는다
             ├ 영역 4
렌즈 중심 ─┘ 세종시 1       모든 축이 여기로 수렴한다

총 410개 노드 / 662개 연결
```

왼쪽 원환 = 공간·환경·농업 + 경제·산업·기술 / 오른쪽 원환 = 시민의 삶 + 도시위상·문화·안전.

### 선은 누르기 전까지 그리지 않는다

기본 화면은 **점만 있는 성좌**다. 노드를 누르는 순간 그 노드의 망만 드러난다.
662개 선을 늘 그리면 아무것도 읽히지 않기 때문이고, 덕분에 노드를 410개까지
늘려도 화면이 무너지지 않는다. 선이 숨겨진 동안은 선 계산 자체를 건너뛴다.

각 원환은 정확히 6개 섹터 = 2개 영역을 담는다. 겹침부(교점 ±126.9°)는 비워
렌즈를 살리고, 노드 좌표와 배경 도형이 같은 상수를 쓰므로 궤도와 정확히 맞물린다.

노드를 선택하면 연결된 노드가 **홉 거리 순서로 차례차례 점등**되고, 그때만 선이 나타난다.
1홉 0.105초, 2홉 0.210초… 순으로 빛이 번지고, 선을 따라 전류가 흐른다.

### 연결의 종류

| 유형 | 수 | 뜻 |
|---|---|---|
| `converge` | 409 | 팀→과→섹터→영역→시 로 모이는 수렴축 |
| `resolves` | 111 | 공약이 어떤 진단을 겨냥하는가 |
| `affinity` | 85 | **서로 다른 섹터에 흩어져 있지만 같은 문제를 공유하는 진단 쌍** (문자 유사도로 도출, 진단당 최대 2개) |
| `synergy` | 38 | 함께 추진하면 효과가 커지는 공약 |
| `dependency` | 16 | 앞 공약의 성패가 뒤를 좌우 |
| `conflict` | 3 | 예산·부지·목표가 부딪힘 |

---

## 실행

```bash
node server.js
```

→ http://localhost:4173 · 의존성 없음. Node 18 이상이면 `npm install` 없이 바로 뜬다.

---

## 정밀 진단 — 97개 전수 채점

세종시는 특정 분야가 아니라 **전 분야가 동시에 취약한 구조**다.
그 판단을 주장이 아니라 수치로 만들기 위해 4축 평가모델로 97개 진단을 전수 채점했다.

### 4축 (각 1~5점)

| 축 | 묻는 것 | 가중 |
|---|---|---|
| **구조성** | 단년도 정책으로 해소되지 않는 구조적 결손인가 | 30% |
| **체감도** | 시민 생활에 직접·즉시 닿는가 | 25% |
| **악화성** | 방치하면 스스로 악화되고 다른 영역으로 번지는가 | 25% |
| **권한 결여** | 해법이 시 권한 밖(중앙·국회·타기관)에 있는가 | 20% |

**구조적 심각도 = 가중합 × 20** (0~100)

### 잔여위험 — 대응을 감안하고도 남는 위험

```
잔여위험 = 구조적 심각도 × (1 − 대응 감쇄) + 현장 신호 가산
대응 감쇄 : 공약 0건 0% · 1건 18% · 2건 30% · 3건 이상 38%
신호 가산 : min(12, 신호부하 × 0.6)
```

공약 하나가 구조적 문제를 다 없애지는 못한다는 전제를 감쇄율에 반영했다.

| 상태 | 잔여위험 | 색 |
|---|---|---|
| **위험** | 72 이상 | 크림슨 `#d42f37` |
| **경계** | 58~71 | 엠버 `#de602f` |
| **주의** | 44~57 | 골드 `#d4a73e` |
| **관리 가능** | 44 미만 | 세이지 `#628c6a` |

### 전체 결과

**평균 구조 심각도 73 / 평균 잔여위험 63. 97개 중 51점 미만은 하나도 없다.**

| 상태 | 진단 수 |
|---|---|
| 위험 | **30** |
| 경계 | **32** |
| 주의 | 30 |
| 관리 가능 | 5 |

### 섹터별 순위 (잔여위험 순)

| 순위 | 섹터 | 구조심각도 | 잔여위험 | 위험진단 | 미대응 | 공약 |
|---|---|---|---|---|---|---|
| 1 | ⑦ 보건·의료서비스 | 83 | **75** | 5/8 | 3 | 4 |
| 2 | ⑥ 복지·사회문제 | 75 | **72** | 3/7 | 3 | 4 |
| 3 | ① 도시 인프라·정주환경 | 79 | **68** | 4/10 | 1 | 5 |
| 4 | ③ 행정수도·정치 거버넌스 | 78 | **67** | 3/8 | 1 | 4 |
| 5 | ⑫ 안전·재난관리 | 74 | **66** | 2/6 | 0 | 4 |
| 6 | ⑤ 교육·인재양성 | 76 | **65** | 3/8 | 3 | 4 |
| 7 | ⑧ 환경·지속가능성 | 71 | **64** | 3/8 | 4 | 4 |
| 8 | ⑩ 과학기술·디지털 혁신 | 68 | **60** | 2/7 | 3 | 5 |
| 9 | ⑨ 농업·도농상생 | 68 | **60** | 2/9 | 2 | 4 |
| 10 | ② 경제·산업·자족기능 | 80 | **59** | 2/10 | 1 | 7 |
| 11 | ④ 문화·예술·도시정체성 | 61 | **54** | 1/10 | 5 | 5 |
| 12 | ⑪ 국제화·글로벌 도시 전략 | 61 | **47** | 0/6 | 1 | 4 |

읽는 법 두 가지.

- **⑦ 보건·의료**는 구조심각도도 1위(83), 잔여위험도 1위(75). 진단 8개 중 5개가 위험이고
  공약은 4개뿐이다. 가장 시급한 축.
- **② 경제·산업**은 구조심각도 2위(80)인데 잔여위험은 10위(59)로 내려간다.
  **공약 7개**가 가장 두껍게 깔려 있기 때문이다. 진단이 가벼워서가 아니라 대응이 두꺼워서다.

### 공개 지표로 뒷받침된 항목

채점 전체는 구조화된 분석 판단이지만, 14개 항목은 공개 수치에 근거한다.
화면에서 해당 진단을 선택하면 근거와 출처가 함께 표시된다.

| 진단 | 근거 |
|---|---|
| 상급종합병원 부재 | 관내 의료이용률 **0.0%** (전국 평균 63.7%) — 사실상 100% 관외 원정진료 |
| 상가 공실과 상권 붕괴 | 중대형 상가 공실률 **24.2~25.8%, 전국 17개 시도 1위** |
| 수도권 의존 심화 | 자족기능용지 비율 **6.7%** — 수도권 신도시보다 낮음 |
| 고교생 역외 전학 | 2025년 인구 **순유출 −47명** — 출범 13년 만에 순유입 구조 붕괴 |
| 매몰비용 증가 | 2026년 채무 5,248억·채무비율 22.30%. 의무지출 56%→**72%** |
| 문화예술 인프라 부족 | 아트센터 **1,071석** — 당초 계획 2,000석의 절반 |
| 지역 내 대학 부재 | 관내 4년제 종합대학 없음 |

---

## 화면

| 영역 | 내용 |
|---|---|
| 상단 | 로고(→메인) · 검색 · 보기모드(전체망 / **공약 공백** / **위험 진단** / 상충축) · 초기화 · **＋신규 정책** · ＋신호 입력 |
| 좌측 | 12대 섹터 — 잔여위험 순 정렬 · 노드종류/행정조직/상태/연결 필터 |
| 중앙 | 맞물린 두 원환 네트워크. 드래그·휠 확대·클릭 점등 |
| 우측 | 선택 노드 상세 — 진단이면 4축 정밀진단·근거·대응공약, 공약이면 해소 진단·목표지표 |

- **공약 공백** — 겨냥한 공약이 없는 진단 27개만 남긴다
- **위험 진단** — 잔여위험 72 이상 30개만 남긴다

---

## 행정조직 연결

12대 섹터를 세종시 행정조직에 붙였다. 좌측 필터의 **소관 과 표시**를 켜면
각 섹터 바깥 궤도에 소관 과가 나타나고, 섹터를 선택하면 상세 패널에
국 → 과 → 팀이 계층으로 펼쳐진다.

| | 수 | 출처 |
|---|---|---|
| 국·실·본부 | 12 | **시 공식 조직도** |
| 과·담당관 | 59 (그래프 노드 76) | **시 공식 조직도 + 부서별 담당업무** |
| 팀 | 166 | **도출값** — 공식 조직도에 팀명이 공개돼 있지 않아 각 과의 담당업무에서 기능 단위로 나눈 것이다. 실제 팀명과 다를 수 있다 |

과가 여러 섹터에 걸치면 섹터마다 노드가 생겨 59과 → 76개 노드가 된다.
팀은 중복을 피해 과가 처음 등장한 섹터에만 매단다.

라벨은 배율에 따라 단계적으로 드러난다 — 기본 배율에서 244개(시·영역·섹터·진단·공약·과),
배율 0.55 이상에서 팀까지 **410개 전부**. 점 자체는 언제나 전부 그려진다.

과 → 섹터 배정은 담당업무 기준이며, 한 과가 여러 섹터에 걸칠 수 있다
(예: 지능형도시과 → 과학기술·디지털 + 도시 인프라).

---

## 신규 정책 입력

상단 **＋ 신규 정책** 으로 정책 니즈를 넣으면, 97개 진단과 대조해 연결 지점을 찾아
망에 편입한다. 자동 판정이 애매하면 저장만 하고 Claude Code 로 넘긴다.

```
정책 니즈 입력 → 망과 대조 → 연결 지점 확인(체크박스) → 편입
                     └ 애매하면 → data/inbox/<ID>.json → Claude Code
```

### 자동 연결 방식

형태소 분석기 없이 **문자 바이그램 Dice 유사도**로 진단·공약·섹터와 대조한다.
절대 임계값만 쓰면 표현이 다른 정책을 통째로 놓치므로, **최고 점수 대비 상대 기준**
(강한 일치 = 최고점의 45%, 최소 0.09)을 함께 둔다.

어림값이므로 자동 연결 결과는 **체크박스로 확인받은 뒤** 반영한다.
예를 들어 "읍면 순환 마을버스"는 "생활교통망 단절"과 함께 "디지털 격차 확대"도
끌어온다 — 두 진단이 모두 '읍면지역'을 담고 있기 때문이다. 이런 건 체크를 풀면 된다.

### Claude Code 로 넘기기

```bash
node tools/link-proposal.js                    # 대기 목록
node tools/link-proposal.js --show <ID>        # 전체 맥락 + 섹터/진단 후보
node tools/link-proposal.js --apply <ID> --sector S07 --resolves S07-02,S07-03
node tools/link-proposal.js --apply <ID> --auto   # 자동 분석 그대로 반영
```

`--show` 는 정책 원문, 섹터 후보 순위, 점수가 매겨진 진단 목록, 12대 섹터 전체를
한 화면에 출력한다. 그대로 판정 근거로 쓸 수 있다.

---

## 디자인 규칙

### 상태는 색이 아니라 '채움 밀도'가 먼저 말한다

모든 노드는 **속이 빈 링**이고, 안쪽 코어가 차오르는 정도로 위험을 읽는다.
색을 전혀 못 보는 상태에서도 상태가 그대로 읽힌다.

| 상태 | 코어 채움 | 색 |
|---|---|---|
| 관리 가능 | 20% | `#628c6a` |
| 주의 | 36% | `#d4a73e` |
| 경계 | 54% | `#de602f` |
| 위험 | 72% + 이중 링 + 맥동 | `#d42f37` |

### 색 — 골드 기조, 연결은 블루

| 역할 | 색 |
|---|---|
| **값** | 골드 램프 7단계 `#5c4a28` → `#ffc75a`. 값이 클수록 밝다 |
| **연결(점등)** | `#43acfb` — 노드를 누르면 연결된 것이 블루로 물든다 |
| **위험** | `#e13b86` 크림슨 |

노드 라벨은 **`값:이름`** 형식이다(`91:상급종합병원 부재`, `1:언론협력팀`).
작은 점도 예외 없이 값을 내건다. 값의 뜻은 계층마다 다르다 —
진단·섹터·영역·시는 잔여위험, 공약은 해소 진단 수, 과는 팀 수, 팀은 연결 수.
4대 영역은 색이 아니라 **원환 위의 위치**로 구분한다.

> **위험색이 왜 순수 적색이 아닌가.** 골드를 기조로 삼으면 순수 적색은
> 적록색각이상에서 골드와 ΔE 2.0 — 사실상 같은 색이 되어 경보가 무력화된다.
> 색상 350°~20°를 전수 검증해, 붉은 계열을 유지하면서 골드 램프 전 단계와
> ΔE 8.9 이상 떨어지는 `#e13b86` 을 골랐다. 여기에 채움 밀도·이중 링·맥동·
> 라벨의 숫자까지 더해 위험은 색 없이도 읽힌다.

### 연결선 — 양끝 굵기가 다른 테이퍼

선은 직선이 아니라 **양끝 폭이 다른 채움 도형**이다.
연결이 많고 큰 노드 쪽이 두껍고, 작은 노드 쪽으로 갈수록 자연스럽게 좁아진다.

```
끝점 반폭 = 0.105 × 반지름 + 0.66 × √(연결 수)     (최대 2.8 : 1 대비)
색       = 양끝 노드 색을 잇는 그라데이션
```

관계 유형(선후의존·상충)은 채움 위에 파선·점선 **중심선**을 덧그려 구분한다.

---

## 데이터

```
data/
├── taxonomy.json    4대영역 · 12섹터 · 상태정의 · 연결유형
├── diagnoses.json   97개 진단 — 원문 + 4축 평가(assess/severity/evidence)
├── pledges.json     54개 공약 — 브리핑 계획표 + 공약자료집
├── links.json       57개 공약 간 관계
├── signals.json     30건 시민 신호
├── org.json         행정조직 12국 / 59과 / 166팀 + 섹터 연결
├── proposals.json   신규 정책 제안 원본 (입력 시 생성)
└── inbox/           Claude Code 가 처리할 대기 건
```

```
tools/
├── autolink.js        바이그램 유사도 자동 연결 엔진
└── link-proposal.js   대기 제안 조회·판정·반영 CLI
```

### ⚠️ 원자료와 분석값 구분

| 구분 | 출처 | 신뢰도 |
|---|---|---|
| 12대 섹터, 97개 진단 문구 | 「진단 12대 섹터 정리」 | **원자료 그대로** |
| 공약명·차수·목표지표 | 브리핑 계획표, 공약자료집 | **원자료 그대로** |
| evidence 14건 | 공개 통계·언론 보도 | **출처 명시** |
| **4축 채점 (97건)** | — | **구조화된 분석 판단. 검토 필요** |
| **공약 → 진단 매핑** | — | **추정. 검토 필요** |
| **공약 간 관계** | — | **추정. 검토 필요** |
| 시민 신호 30건 | — | **예시. 실제 민원으로 교체 필요** |
| 국·과 이름과 담당업무 | 시 공식 조직도 | **원자료 그대로** |
| **팀 166개** | — | **도출값. 공식 팀명과 다를 수 있음** |
| **과 → 섹터 배정** | — | **담당업무 기준 배정. 검토 필요** |

잔여위험 수치는 채점과 매핑 위에 얹혀 있다. 둘을 고치면 순위가 달라진다.

---

## API

| 메서드 | 경로 | 설명 |
|---|---|---|
| `GET` | `/api/graph` | 분류체계·진단·공약·관계·신호 일괄 |
| `POST` | `/api/signals` | 시민 신호 추가 (`title`, `targets[]`=진단ID) |
| `POST` | `/api/pledges` | 공약 추가 (`label`, `sector`, `resolves[]`) |
| `POST` | `/api/diagnoses` | 진단 추가 |
| `POST` | `/api/links` | 공약 간 관계 추가 |
| `POST` | `/api/proposals/analyze` | 신규 정책을 망과 대조만 (저장 안 함) |
| `POST` | `/api/proposals` | 신규 정책 저장 + 편입, 또는 Claude Code 로 인계 |
| `GET` | `/api/proposals` | 제안 목록 |

---

## 다음 단계

1. **채점 검토** — 4축 점수는 분석 판단이다. 현장 감각으로 조정해야 순위가 신뢰를 얻는다
2. **매핑 검토** — `resolves` 가 이 도구의 정확도를 좌우한다
3. **팀 정보 교체** — 166개 팀명은 도출값이다. 실제 조직도로 바꾸면 책임 소재가 정확해진다
4. **공약 보강** — 공약2(경제정책 발표본)는 이미지 슬라이드라 미반영
5. **실제 민원 연결** — 국민신문고·새올 데이터 투입
6. **자동 연결 고도화** — 바이그램은 어림값이다. 형태소 분석이나 임베딩을 쓰면 정확도가 오른다
7. **당선 후 전환** — 공약에 실행 진척률·예산을 붙이면 그대로 시정 모니터링 도구가 된다
8. **저장소** — JSON 파일 → DB
